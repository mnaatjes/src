<?php
    /*----------------------------------------------------------*/
    /**
     * date_build - Build a date from parameters
     * 
     * @param int $year - 4 digit format, Default current year
     * @param int $month - 2 digit format, Default current month
     * @param int $date - 2 digit format, Default current date
     * @param array [$options] - Configuration and output options
     * 
     * @return int - UNIX date format
     */
    /*----------------------------------------------------------*/
    function date_build(int $year, int $month, int $date, array $options){

    }
?>